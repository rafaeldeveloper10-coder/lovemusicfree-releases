# lovemusicfree-releases

Canal público de actualizaciones para LoveMusicFree — solo contiene
`latest.json` (versión más reciente) y los APK de Android como
releases adjuntos. No hay código fuente acá; el código de la app vive
en un repo privado aparte.

La app chequea `latest.json` para saber si hay una versión nueva.
